name: seu_projeto
description: A new Flutter project.

publish_to: 'none' # Remove esta linha se você quiser publicar para o pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Outras dependências
  font_awesome_flutter: ^10.0.0
  url_launcher: ^6.0.20

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/imagem_perfil.jpg
